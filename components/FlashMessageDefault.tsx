
// The flash message library will pass an object of props describing the message.
// We adapt that object into our `DismissibleBanner` props. This component will be
// passed as `MessageComponent` to the global <FlashMessage /> so all showMessage
// calls render our banner and the API remains compatible.
export default null;
